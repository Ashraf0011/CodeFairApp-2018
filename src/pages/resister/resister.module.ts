import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { ResisterPage } from './resister';

@NgModule({
  declarations: [
    ResisterPage,
  ],
  imports: [
    IonicPageModule.forChild(ResisterPage),
  ],
})
export class ResisterPageModule {}
